import React from 'react';
import '../styles/task.css'

const Task = ({ title, assignee, status, onAssigneeChange, onStatusChange }) => {
    const assigneeOptions = ["John", "Jane", "Bob", "Alice"];

    return (

        <div></div>
    );
}

export default Task;
